using System;
using System.Collections.Generic;
using System.Configuration;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace JinnSports.Parser.App
{
    static class ConfigSection
    {
       /*public static string XmlPath()
        {
            System.Configuration.Configuration config = ConfigurationManager.OpenExeConfiguration(ConfigurationUserLevel.None);
            var section = config.Sections["ProxyXml"];
            if (section == null)
                throw new InvalidOperationException("No ProxyXML section in config file");
            var client = section as ClientSettingsSection;
            var xmlPath = client.Settings.Count;
            return "";
        }*/
    }
}
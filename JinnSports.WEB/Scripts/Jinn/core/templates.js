'use strict';

function Template(parameters) {

}

Template.prototype = {
    // Generates dom from template using data
    apply: function (tmpl, data) {

    },

    // Returns data from DOM using template
    parse: function (tmpl, domElement) {
        var data = {};

        return data;
    }
};